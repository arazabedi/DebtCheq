import React from 'react';
import {View, StyleSheet} from 'react-native';

const Cheque = () => {
	const { data, isLoading, error } = UseFetch();
	return (
		<View>

		</View>
	);
}

export {Cheque};
